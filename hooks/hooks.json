{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "description": "iroha-for-notion: inject the project's prior State + a save reminder at session start, and proactive local recall (pure-jq BM25 over the keys-only index, no LLM/network) of relevant past decisions on each prompt.",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume|compact",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh\"",
            "timeout": 5
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"${CLAUDE_PLUGIN_ROOT}/hooks/recall-inject.sh\"",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
